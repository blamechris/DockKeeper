# DockKeeper — Test Strategy

| | |
|---|---|
| **Status** | Draft for review |
| **Date** | 2026-07-22 |
| **Owner** | blamechris |
| **Scope** | v1.0 verification: unit, integration, manual hardware matrix, reliability |
| **Inputs** | [Behavior specification](behavior-specification.md), [Technical design](technical-design.md) (Appendix B, §4.3 test seams), kickoff package §7 |

Evidence labels: **CONFIRMED** · **INFERRED** · **PROPOSED** · **UNKNOWN**.

## Principles

- **Every behavioral requirement maps to one or more tests** (traceability table below). A requirement without a named test is a gap, listed as such.
- **macOS side effects live behind adapters; decision logic is pure.** We do not force unit tests around untestable side effects (moving the real Dock, `SMAppService`) — we wrap them and test the decisions (kickoff §7 rule). `MainDisplayPinner.decide` already follows this pattern (CONFIRMED); `DockAdapter` is the priority missing seam (TDD §4.3).
- **Behavior-focused test names**, e.g. `testDuplicateDisplayEventsProduceSingleRecoveryAttempt()`.
- **TDD development rule** for implementation work: (1) add/update the requirement → (2) write the failing test → (3) smallest passing change → (4) refactor → (5) run the relevant suite → (6) update docs when behavior or architecture changed.
- Framework: Swift Testing (`import Testing`, `@Suite`/`@Test`) — CONFIRMED in use.

## Test levels

### 1. Unit tests (fast, no system effects)

Targets: state reconciliation (`RecoveryCoordinator.decide` with synthetic events + simulated clock), display fingerprint matching/scoring/tie-handling/repair/migration, retry ladder and backoff, debounce/coalescing (generation counter), cooldown/oscillation budget, echo suppression, failure classification, orientation model mapping, settings persistence and migration, CLI argument parsing, `LoginItemManager.message(for:)` mapping.

Required seams (TDD §4.3): `DockAdapter` protocol with a recording fake (**missing in v0.1 — build first**); injected `applyMain` closure (exists — CONFIRMED); typed `DockEvent` values (post-M3 refactor); injectable clock for ladder/cooldown tests (PROPOSED).

### 2. Integration tests (real frameworks, isolated state)

Preferences persistence against isolated `UserDefaults` suites (pattern exists in `SettingsTests` — CONFIRMED); UUID→fingerprint migration on a populated suite; coordinator + monitor with mocked adapters driven by synthetic event sequences; CLI end-to-end against an isolated defaults domain; external-defaults-change observation (DK-FR-007-S3).

### 3. Manual system tests (hardware matrix)

**Blocked on a 2-monitor rig — the M6 gate and top project risk (R-002).** Adapted from kickoff §7; Accessibility rows dropped (no permission in v1 — TDD §10).

| Dimension | Cases |
|---|---|
| Topology | 1 display · 2 displays · 3+ · built-in + external · two identical externals (identity tie → must ask, never guess) |
| Connection | DisplayPort · HDMI · USB-C · docking station (UUID stability across each — R-003) |
| Events | disconnect · reconnect · sleep/wake · rapid replug · resolution change · arrangement change · Dock process restart (`killall Dock`) · reboot · logout/login |
| Modes | separate Spaces ON (pin declined + explained) and OFF · mirroring (UNKNOWN correct behavior — spike) · clamshell · fullscreen apps · Stage Manager · Dock autohide |
| Edges | bottom · left · right, each surviving every event above |
| Pinning | pin moves Dock + menu bar (verify the INFERRED core claim!) · preferred display absent → no fallback pin · reconnect → re-pin |
| Login item | approval flow, `.requiresApproval` deep link, packaged-app vs `swift run` |

Each cell records: pass/fail, macOS version, hardware path, and any drift latency observed. Results feed the risk register and ADR-005's poll-interval evidence.

### 3b. Manual instance tests (no hardware gate)

**Single-instance guard (DK-FR-012 / [ADR-012](decision-log.md#adr-012-single-instance-guard-in-process-at-appinit-lsmultipleinstancesprohibited-withheld)), in risk order.** Deliberately **not** part of §3: none of these needs a second monitor, so they are runnable today on one Mac plus one extra user account and must not sit behind the R-002 hardware gate. **Partially run 2026-08-18** (macOS Darwin 25.6.0, installed 0.9.2, evidence on [#30](https://github.com/blamechris/DockKeeper/issues/30)): rows 6, 7, 7b, 7c, 8 and 9 **pass**. Row 6 now passes in **both** variants: its cross-path half was run in a second pass on 2026-08-18 against a `dist/` bundle stamped 0.1.0: `Bundle path:` named the reporter's `dist/` copy while `Other instances:` named `/Applications/DockKeeper.app`, which is precisely the discrimination the row exists to prove. The differing version stamps made the two bundles easy for the *tester* to keep apart; they are **not** what the report discriminates on — see row 6 for what it does and does not carry. Row 4 is **half-confirmed** (its pause half was unobservable when the matrix was run; [#36](https://github.com/blamechris/DockKeeper/issues/36) has since been fixed — ADR-014 gives pause a durable record and `status`/`--diagnostics` a `Paused:` line, so the row now has a stated verification method and is re-runnable); rows 1, 2, 3 and 5 remain outstanding, along with row 4's re-run. What those five have in common is the reason they are still open: each needs the incumbent out of the way — a second account (1), a logout (2), or the `/Applications` copy quit so a `dist/` build can be the senior instance (3, 5) or the registered `dockkeeper://` handler (4). Rows 6, 8 and 9 were runnable precisely because none of them needs the incumbent *displaced* — which is not the same as leaving it undisturbed. Row 8 moves the live Dock edge and writes to the defaults domain; it simply does so without the app having to stop. The pure decision was already exhaustively unit-tested and the LaunchServices behaviors probe-CONFIRMED; what row 7c adds is the first exercise of the **assembled** guard against a dateless incumbent — the ADR-012 Decision itself, previously INFERRED.

| # | Case | Expected | Why it ranks here |
|---|---|---|---|
| 1 | **Second user account, fast user switching.** With DockKeeper running in session 1, switch to a second user and launch it there. **No logout required** — fast user switching is precisely the two-simultaneous-sessions mechanism; only row 2 needs a logout. Also run `DockKeeper --diagnostics` in session 2 and expect the session-1 instance listed as `(another user)` | Both sessions get their own instance | **Downgraded 2026-08-18: no longer a gate on the shipped guard** ([ADR-012 Amendment](decision-log.md#adr-012-single-instance-guard-in-process-at-appinit-lsmultipleinstancesprohibited-withheld)). It was a gate while per-session scoping was INFERRED from `NSRunningApplication` — which Apple DTS says is **undefined** across multiple GUI login sessions. Rather than settle undefined behaviour with one observation, the guard now filters peers by uid and the property is a unit-tested invariant of the pure core. This row is now an end-to-end confirmation of a proven model, and still gates the `LSMultipleInstancesProhibited` follow-on — a LaunchServices *launch policy* the uid filter does not touch: refused there → WONTFIX, recorded in ADR-012 |
| 2 | **Login-item path.** Log out and back in with the login item registered | Exactly one instance; no stderr notice in the log from a self-deflection | The one launch path no developer exercises by hand; also whether an `SMAppService.mainApp` launch routes through LaunchServices at all is UNKNOWN (untestable without logging out) |
| 3 | **`Scripts/run-app.sh` twice in a row** while the app is running | Exactly **one** process afterwards, running the **newly built** binary. If the previous build ignores `SIGTERM`, the script escalates to `SIGKILL` and, failing that, **exits non-zero** instead of rebuilding | This is the second duplicate vector and the primary script fix. New row — §3 previously had no `run-app.sh` case at all; the investigation's initial belief that the script harmlessly relaunched the stale binary was refuted by the control experiment in ADR-012 |
| 4 | **`open "dockkeeper://pause"`** while running | The pause takes effect on the running instance; nothing is spawned or deflected. **Verify the pause half with `dockkeeper status`** — expect `Paused:     yes (until resumed — no timer)`, and `Paused:     no` again after `open "dockkeeper://resume"`. Verify the not-spawned half by counting pids, not by `open`'s exit status | Regression guard on DK-FR-010: a plain URL open is a Path-B open LaunchServices routes to the running app (`allowsRunningApplicationSubstitution` defaults true) — the guard must not break automation. The pause half was **unobservable by any non-visual means** until ADR-014 gave pause a durable record ([#36](https://github.com/blamechris/DockKeeper/issues/36)); the CLI reads that record from its own process, so this row no longer needs a human looking at the menu bar. While here, also run DK-FR-009 S7: kill the app while paused, relaunch, and expect `Paused:     no` — a restart is an implicit resume |
| 5 | **`open /Applications/DockKeeper.app`** while `dist/` runs | One icon survives, and it is the **first** one — even though it may be the older version | The observed duplicate vector, and the explicit test of "senior, not newest" (DK-FR-012 Failure behavior) |
| 6 | **`dist/DockKeeper.app/Contents/MacOS/DockKeeper --diagnostics`** while running | The report still prints, and `Other instances:` names the running copy and its bundle path | `--diagnostics` is the documented support flow and is run *while* an instance is live; if the guard pre-empts it, support loses its only view of the duplicate. **The `dist/` path is load-bearing, not incidental**: reporter and incumbent must be *different* bundles, or `Other instances:` naming its own path is indistinguishable from naming the peer's — and telling the two bundles apart is the whole reason support reads that field. A same-bundle run confirms only the not-pre-empted half; the cross-path half needs a second bundle. **Both CONFIRMED 2026-08-18**, the cross-path half in a second pass: a `dist/` bundle stamped 0.1.0 reporting against the installed 0.9.2 printed its own path on `Bundle path:` and the incumbent's on `Other instances:`, and was not pre-empted. Note what the report still does **not** carry: `Version:` is the *reporter's*, and no version is shown for the peer — so support can tell the two bundles apart by path but not by version |
| 7 | **Direct exec of the inner binary** (no `--diagnostics`) while a registered copy runs | Exits with the stderr notice, no second icon | The no-LaunchServices-date case as a **newcomer** |
| 7b | **The mirror of 7: direct exec FIRST**, then `open /Applications/DockKeeper.app` | One survivor, and it is the direct exec. Note **nothing is spawned** to be deflected | Verifies **LaunchServices substitution**, *not* the guard — the claim this row carried until 2026-08-18. A direct-`exec`ed binary checks in with LS as a running instance of **that bundle**, so an `open` naming the same bundle is substituted onto it instead of launching (inode-keyed — see the note under the table; this is why row 5, a *different* bundle, still reaches the guard): 120 `ps` samples at 20 Hz across the `open` caught no second pid, and `lsappinfo` shows the incumbent's checkin. `SingleInstance.yieldIfDuplicate()` is therefore never reached and no seniority comparison happens. Kept — "one icon survives and it is the first one" is worth pinning — but the ADR-012 Decision is row 7c's job |
| 7c | **Direct exec of a second copy while a direct-`exec`ed (dateless) incumbent runs** | The newcomer exits with the stderr notice naming the incumbent; one survivor, the incumbent, with an unchanged start time | **The end-to-end gate on the ADR-012 Decision, and the only row that reaches it.** Both processes bypass the LS launch path, so the guard actually runs — and the incumbent's `NSRunningApplication.launchDate` is `nil`, making `SingleInstance`'s **peer** substitution (`startTime(of:) ?? $0.launchDate`) load-bearing. Without it the dateless incumbent ranks `(1, .distantPast, pid)`, any dated newcomer ranks `(0, now, pid)` and wins, and **both** run — verbatim the regression ADR-012 records. Row 7 covers only **self** substitution: its incumbent is LS-launched and *has* a date. Needs no build, no second account, no logout |
| 8 | **`dockkeeper lock left`** from the CLI while the app runs | Unchanged — the command applies, nothing is deflected | `dockkeeper-cli` is a legitimate third process; the guard must never reach it (ADR-012 Consequences). **CONFIRMED 2026-08-18** — and confirmed on the *live* Dock, not just in settings: `lockEdge` went 2 → 3 and `com.apple.dock orientation` followed `bottom` → `left`, then both restored, with the app's pid unchanged in all three samples spanning the two commands. An unchanged pid is the actual assertion here; a deflected CLI would have had to exit, and a restarted app would have had to change pid. **"Unchanged" means the app and the guard, not the defaults domain**: `lock` also does `settings.isEnabled = true` unconditionally ([main.swift](../Sources/dockkeeper-cli/main.swift)), so running this row materialises `enabled` in `com.dockkeeper.app` if it was previously relying on the registration default. Effective value is unchanged either way — but a run that diffs defaults before and after will see that one key appear, and it is the CLI's documented enable-and-lock semantics, not a leak |
| 9 | **`swift run DockKeeper`** while the `.app` runs | **Two instances, by design** — confirm no crash and that the behavior matches DK-FR-012 S7 | Documents a known, deliberate hole rather than discovering it later as a defect. **CONFIRMED 2026-08-18**, both halves of S7 — *not blocked* (the unbundled binary ran alongside the installed 0.9.2 for the full window, empty stderr, no guard notice, dying only on the test's own `SIGALRM`) and *not detected* (a third process running `--diagnostics` **during the overlap** listed only the bundled incumbent). Also worth recording because it is the reason this row is cheap to run: `orientation` and `lockEdge` were unchanged for the whole overlap, so nothing visible happened to the Dock. **Do not read that as "the two instances do not contend"** — the window was quiescent, with the Dock already sitting at the locked edge and nothing perturbing it, so no contention was ever *induced*. Whether two live instances race to correct a Dock that actually moves is untested; it would need the edge changed while both run, watching for double-correction or oscillation. The likely reason they would agree — both reconcile to the same stored `lockEdge` in a shared defaults domain — is read off the source, not measured, and is INFERRED. Run it as a bare `.build/debug` binary under an alarm rather than via `swift run`: the spec names both spellings, and a self-expiring process needs no kill of a GUI app. See the registration note below for *why* both halves hold |

Each row records: pass/fail, macOS version, which bundle paths were registered at the time, and the exit path observed (stderr notice / log line / neither). Note that **`open`'s exit status is not evidence** — it returns 0 whether the launch survived or was deflected (measured), so every row must be checked by counting menu-bar icons or live pids. **A deflected launch also exits 0** (measured 2026-08-18; `SingleInstance` exits `EXIT_SUCCESS` deliberately, so a login item that correctly deferred does not read as a failed launch), so exit code cannot distinguish "ran" from "yielded" either.

Two measured properties of a directly-`exec`ed bundle binary drive the design of rows 7–7c and are easy to get backwards.

- **It registers.** `lsappinfo` reports it with an ASN, `bundleID`, `bundle path`, `type="UIElement"` and a checkin time — it is a first-class running instance of *that bundle*, not an anonymous process. So an `open` naming the **same bundle** is substituted onto it rather than launching (row 7b), and the guard never runs. This does **not** generalise to row 5: LaunchServices keys its already-running test on the bundle's **inode**, so a second copy at a different path (`dist/`) is a different application, is not substituted, and does reach the guard. Same-bundle and different-bundle are different tests, which is why 7b and 5 both exist.
- **It stays dateless.** Its `NSRunningApplication.launchDate` is `nil` for its **entire lifetime**, so a peer that is one of these must have a kernel start time substituted before it can be ranked at all (row 7c).

Registered-but-dateless is one state, not two, and a row that exercises only half of it tests something other than what it claims — which is exactly how 7b's stated purpose drifted from its actual coverage.

A **third** registration state, measured 2026-08-18 while running row 9, completes the set — and it is what collapses both halves of DK-FR-012 S7 into a single fact rather than two independent ones. An unbundled build *also* checks in with LaunchServices: `lsappinfo` gives it an ASN, `type="UIElement"`, a bundle path pointing at the bare binary, and a checkin time. What it does **not** get is an identifier — `bundleID=[ NULL ]`, `Version=[ NULL ]`. From that one omission both halves follow:

- **Not blocked**, because `SingleInstance.yieldIfDuplicate()` returns early on `Bundle.main.bundleIdentifier == nil` — it never reaches a comparison.
- **Not detected**, because `NSRunningApplication.runningApplications(withBundleIdentifier:)` has no identifier to match it against. Confirmed from the outside, which is the only way it can be: a *third* process running `--diagnostics` during the overlap listed the bundled incumbent and not the unbundled peer, though both were alive at that instant.

So the three states are: **LS-launched bundled** (identifier, path, `launchDate`), **direct-`exec`ed bundled** (identifier, path, no `launchDate` — rows 7–7c), and **unbundled** (no identifier at all, hence invisible to every bundle-ID query — row 9). Reading "unbundled" as "unregistered" is the easy mistake, and it is wrong: it registers, it simply registers anonymously. The distinction matters for any future guard that reasons about peers by something other than bundle identifier — such a guard would start seeing `swift run` builds, and DK-FR-012 S7 says it must not.

Two cautions for whoever runs the rest of this matrix, both learned the hard way:

- **Probe bundles outlive the session that made them.** A `Probe.app` from an earlier session was still checked in with LaunchServices, and still running, 37 minutes after that session ended. It was harmless only because it claimed `com.example.dkprobeapp`; a probe that reuses `com.dockkeeper.app` would have silently contaminated every row above. Check `lsappinfo list | grep -B2 'bundleID="com.dockkeeper.app"'` before trusting a result, and expect exactly one entry.
- **Rows 6, 8 and 9 need no build gate and no second account.** They were previously grouped with the blocked rows; they are not. Only rows 1, 2, 3, 5 and row 4's re-run require the incumbent to be displaced.

### 3c. Manual termination / crash-recovery tests (no hardware gate)

**Borrowed Dock auto-hide across process death (DK-FR-013 / [ADR-013](decision-log.md#adr-013-borrowed-system-state-is-persisted-before-the-borrowing-write-and-reconciled-at-launch-termination-hooks-are-an-optimization-not-the-mechanism)).** Everything below is outside the unit suite **by construction**: the test target links only `DockKeeperCore` and never calls a private symbol or moves the real Dock, so the pure `repair` table and the port-driven integration tests prove the *decisions* and nothing else. **Not yet run** — every row here is **INFERRED**, and none of it may be quoted as CONFIRMED before it passes. Rows 3 and 4 are the gate on the issue #29 fix itself; row 1 is the standing DK-FR-011 unknown that every other row sits on top of.

| # | Case | Expected | Why it ranks here |
|---|---|---|---|
| 1 | **A real capture flips `CGSIsScreenWatcherPresent`** (QuickTime / Zoom / Teams / Screen Sharing.app) | The flag goes true, the Dock auto-hides, and stopping the capture restores it | The standing ADR-011 UNKNOWN (M6/M12). Without it **nothing below is exercised on real hardware** — but it is deliberately *not* a blocker on the fix: the defect is reachable whenever the detector does flip, including spuriously, which widens the blast radius rather than narrowing it |
| 2 | **Menu Quit while a hide is held** in the real signed bundle | Auto-hide is off before the process exits; no record survives | The trappable path `applicationWillTerminate` is supposed to cover; measured only against a minimal probe app so far |
| 3 | **`kill -9` mid-capture, then relaunch** — the headline issue #29 cycle | On the next launch auto-hide is really OFF, the record is gone, and the menu really shows the note | The end-to-end proof of the fix. Unit tests prove the decision and the port calls; this proves the whole chain against the real Dock |
| 4 | **Log out with a hide held, log back in** | Auto-hide is off after login | The dominant real exit path, and the one ADR-013 refuses to depend on a hook for. Needs a real logout — untestable any other way |
| 5 | **`kill -TERM` on the installed app**, and `Scripts/run-app.sh` stopping the previous build | The `TerminationSignals` source runs the restore and the process exits cleanly, before the script's `SIGKILL` escalation | Proves the dev-loop path that made the bug a daily occurrence; also the only check that `SIG_IGN` + `DispatchSourceSignal` behaves in the bundle as it did in the probe |
| 6 | **Relaunch mid-share (`.adopt`)** on a real screen share | The far end never sees the Dock appear | Test-provable only as "no Dock write"; the *visible* outcome is on-device by definition |
| 7 | **The menu item for the already-poisoned population** — a machine whose auto-hide was left on by a pre-fix build | "Turn Off Dock Auto-Hide" appears while the Dock is auto-hiding and the feature is on, and reads clearly | `MenuBarExtra` rendering is not unit-testable here, and this row is the only recovery those users have |
| 8 | **`CoreDockSetAutoHideEnabled` durability across an immediate exit** — does the value survive if the process dies microseconds after the call returns | Value survives | UNKNOWN, on-device only; filed as a follow-on rather than designed around |
| 9 | **`cfprefsd` durability across kernel panic / power loss** | — | **Untestable without panicking the machine.** Recorded as a documented residual, not a planned cell; its failure mode is exactly today's behavior (no regression) and S11 is its recovery |
| 10 | **Cross-user / fast user switching** with a hide held in one session | Sessions do not interfere | Argued from per-user `UserDefaults` and per-user `com.apple.dock`, **not measured**. Pairs with §3b item 1 |

Each row records: pass/fail, macOS version, how the process was ended, and whether the record was present, discarded, or acted on afterwards (`--diagnostics` ▸ `Screen-share:` is the read-only view).

### 4. Reliability tests

100 consecutive restorations (no oscillation, no leak); repeated sleep/wake cycles; rapid display connect/disconnect storms; resolution change during an in-flight restoration; preference change during restoration; app relaunch during pending recovery; 24 h idle (CPU ~0%, memory flat, log growth bounded) — the DK-NFR-001 measurement gate (M6).

### 5. CI gates (PROPOSED)

`swift build && swift test` on a macOS 14 runner; release builds additionally: `Scripts/build-app.sh` succeeds, `codesign --verify` passes, and a **no-networking-symbols check** (scan the binaries for `NSURLSession`/`Network.framework` references) enforcing DK-NFR-002 by construction.

## Requirements → tests traceability

Existing suites (39 tests — CONFIRMED 2026-07-22): `DockOrientationTests` (5), `SettingsTests` (2), `DisplayPinnerTests` (6 — every `decide` branch) in [DockOrientationTests.swift](../Tests/DockKeeperTests/DockOrientationTests.swift); `RecoveryMachine`/`RecoveryCoordinator` suites (31 — decide, ladder, echo, cooldown, coalescing, poll counter, disable teardown, plus DK-FR-009 pause/resume transitions and orchestration) in [RecoveryTests.swift](../Tests/DockKeeperTests/RecoveryTests.swift); `DockControllerTests` (6 — adapter seam, fallback, degraded) in [DockControllerTests.swift](../Tests/DockKeeperTests/DockControllerTests.swift). Automation (DK-FR-010) added `ControlCommandParseTests` (16) + `StatusSummaryTests` (3) in [ControlCommandTests.swift](../Tests/DockKeeperTests/ControlCommandTests.swift). Screen-share hide (DK-FR-011) added `ScreenShareHider.decide` + lifecycle/settings suites (8) in [ScreenShareTests.swift](../Tests/DockKeeperTests/ScreenShareTests.swift); the suite total was **115 tests — CONFIRMED 2026-07-25** (the newest at that point was the `Settings` shared-domain guard, DK-FR-007). The single-instance guard (DK-FR-012) adds an `InstanceGuard.decide` suite — 12 `@Test` cases, one of them a 27-combination survivor sweep — in [InstanceGuardTests.swift](../Tests/DockKeeperTests/InstanceGuardTests.swift), bringing the suite total to **127 tests — CONFIRMED 2026-08-17** (`swift test`: 127 tests in 24 suites). Crash-safe restore of the borrowed Dock auto-hide (DK-FR-013) adds three suites to [ScreenShareTests.swift](../Tests/DockKeeperTests/ScreenShareTests.swift) — `ScreenShareTerminationTests` (6 — the restore `prepareForTermination` invokes, driven directly as `stop()`; the hook itself lives in the app target, which this target does not link), `ScreenShareRepairTests` (4 — the pure repair rule table, the 48-combination safety sweep, one-step convergence, and the 7-day window as an asserted constant), and `ScreenShareRepairIntegrationTests` (18 — write-ahead/write-behind ordering *observed from inside the injected Dock write*, so a reversed-order mutant fails, the no-record launch touching neither private port, the unreadable-auto-hide tick minting nothing, the feature-off exit from a re-hide loop, the kill-then-relaunch repair, `.adopt` issuing no Dock write, the settled-preference and already-off discards, the unreadable-symbol record-preserving case, manual recovery with and without a record, and three `Settings` guards) — for **28 new tests**, bringing the suite total to **155 tests — CONFIRMED 2026-08-17** (`swift test`: 155 tests in 27 suites). Giving pause a durable, cross-process surface (DK-FR-009 S6/S7, ADR-014) adds `RecoveryCoordinatorPauseRecordTests` (7), `PauseRecordSettingsTests` (5) and `StatusSummaryPauseTests` (5) — **17 new tests**, plus the two golden `StatusSummaryTests` cases updated to carry the new `Paused:` line — bringing the suite total to **173 tests**. Note the baseline moved between those two counts: the 155 recorded on 2026-08-17 became **156** when the DK-FR-007 shared-domain fix ([#35](https://github.com/blamechris/DockKeeper/pull/35)) added a case, and 156 + 17 = 173. Hardening the report's duration rendering against the `Int(_: Double)` trap (ADR-014 — a user-writable defaults value could crash `--diagnostics`) adds `DisplayDurationTests` (5), and closing the review gaps on the same PR adds `StatusSummaryLiveTests` (2) plus a timed-pause voice-line case, for a suite total of **181 tests**. Scoping the guard to the current user's session ([ADR-012 Amendment](decision-log.md#adr-012-single-instance-guard-in-process-at-appinit-lsmultipleinstancesprohibited-withheld)) adds `InstanceGuardSessionTests` (9 — the eight session cases plus a most-senior-incumbent case added in review, which pins *which* peer the bail names: swapping `.min` for `.max` in `decide` previously passed the entire suite), bringing the suite total to **190 tests — CONFIRMED 2026-08-18** (`swift test`: 190 tests in 33 suites). Note that `ScreenShareDecideTests`' 8-row ADR-011 table is deliberately **untouched** (CONFIRMED by diff; the only edit in the pre-existing suites is injecting a test `Settings` into a hider construction, so the write-ahead record cannot leak into the real `com.dockkeeper.app` domain) — its staying green is the proof obligation ADR-013 records for the ADR-011 contract.

| Requirement / scenario | Existing coverage | Needed |
|---|---|---|
| DK-FR-001 S1–S3 (restore, idempotence, fallback) | ✅ `DockControllerTests` (fake adapters) + `RecoveryMachineTests` (Degraded transition) | — |
| DK-FR-001 S4 (no top edge) | ✅ `userSelectableExcludesTop`, raw-value mapping | — |
| DK-FR-002 S1, S5 (pin decisions) | ✅ `DisplayPinnerTests` all six branches | Arrangement-preserving origin math; hardware verification (matrix) |
| DK-FR-002 S2/S2b (separate-Spaces gate: bottom declines, left/right pins — ADR-009) | ✅ `unsupportedSeparateSpacesBottom` + `separateSpacesLeftRightPins` | Manual: UI copy shown; matrix cells for left/right-in-mode under wake/replug |
| DK-FR-002 S3–S4 (absent / re-pin on return) | ✅ `displayNotConnected` (decision only) | Event-sequence test: reconnect → re-pin; `PreferredDisplayMissing` state transitions |
| DK-FR-003 S1 (wake ladder) | ✅ `RecoveryTests` (machine + coordinator, simulated clock) | — |
| DK-FR-003 S2 (burst → single attempt) | ✅ `duplicateEventsProduceSingleAttempt` | — |
| DK-FR-003 S3 (echo suppression) | ✅ machine + coordinator echo tests | — |
| DK-FR-003 S4 (oscillation guard) | ✅ `oscillationGuard` / `oscillationStops` | — |
| DK-FR-003 S5 (poll safety net) | ✅ `pollCountsDrift` (unit) | Integration tick test with a real timer |
| DK-FR-004 (enable/disable) | ✅ `SettingsTests` (persistence) + `disableStrandsPasses` (no residual corrections) | Observer-teardown integration test |
| DK-FR-005 (login item) | — | Unit: `message(for:)` mapping; manual: approval matrix row |
| DK-FR-006 (menu bar) | — | Manual; state→copy mapping unit tests where pure |
| DK-FR-007 (CLI) | — | Argument-parsing table test; `status` snapshot; external-change observation (S3) |
| DK-FR-009 S1–S7 (pause / temporary move; observability — ADR-014) | ✅ `RecoveryCoordinatorPauseTests` (strand-on-pause, ignore-events, auto-resume via fake scheduler, manual-resume strands stale timer, second-pause supersedes) + `RecoveryMachineTests` pause/resume transitions incl. from-disabled rejection. **S6's record half** adds `RecoveryCoordinatorPauseRecordTests` (the record follows `machine.state` through untimed pause, timed pause, auto-resume, manual resume, supersede and disable; the change guard writes once for an unchanged record; **nothing at all** is written across repeated reconciles while unpaused — the DK-NFR-001 quietness claim; persistence stays optional), `PauseRecordSettingsTests` (round-trip timed and untimed, an undecodable value degrading to "not paused" rather than a phantom pause, not-registered, not-externally-observed) and `StatusSummaryPauseTests` (the line is present in both states and the two differ — the actual #36 defect; untimed called out as having no timer; deadline rendered; label column aligned; the voice line leads with the pause) | Manual: menu items shown/hidden by enabled state; hotkey registration + system-wide toggle (out of unit scope). **S6's cross-process half is CONFIRMED by measurement** (ADR-014 Evidence — a cold `dockkeeper-cli status` rendering a record it did not write). Only the *separate-process* half needs measuring: the in-process seam is unit-tested (`Settings.pauseRecord` round-trip, and `StatusSummary.live` reading it — the single line whose omission is precisely what left the Shortcuts/Siri path blind). **S7 is INFERRED** — the launch clear is one guarded write in `AppState.init`, which the unit target does not reach; §3b row 4 now carries the on-device cell |
| DK-FR-010 S1–S4 (Shortcuts + URL scheme) | ✅ `ControlCommandParseTests` (16 — full parse table: four commands, edge case-insensitivity, top rejected, minutes present/absent/zero/negative/garbage/non-finite/over-cap, unknown host, foreign scheme, extras ignored) + `StatusSummaryTests` (3 — CLI-line + voice-line parity with `dockkeeper status`) in [ControlCommandTests.swift](../Tests/DockKeeperTests/ControlCommandTests.swift) | Manual (INFERRED, not run on-device): S5 Shortcuts/Siri discovery + URL-open dispatch; App Intents metadata extraction in packaging (out of unit scope) |
| DK-FR-011 S1–S6 (hide Dock during screen capture — ADR-011) | ✅ `ScreenShareHider.decide` truth table (all 8 `capturing × weHidIt × currentAutoHide` combos, incl. never-touch-user-auto-hide, idempotence, teardown-safe) + settings default-false / persist / fresh-hider-no-op / poll-cadence in [ScreenShareTests.swift](../Tests/DockKeeperTests/ScreenShareTests.swift) | Manual / **UNKNOWN pending on-device** (M6/M12): does the `CGSIsScreenWatcherPresent` flag flip on a real capture, latency, which apps (QuickTime/Zoom/Teams/Screen Sharing.app); private-API reads/writes never unit-tested (would move the real Dock). **S6 is scoped**: the CONFIRMED teardown restore covers the feature-off and app-disabled exits only — every other exit is DK-FR-013 |
| DK-FR-013 S1–S11 (restore borrowed Dock auto-hide across process death — ADR-013) | ✅ `ScreenShareRepairTests` (pure `repair`: a 10-row rule table covering every branch plus both window boundaries, a **48-combination** sweep asserting the safety property *never `restore`/`adopt` without observing auto-hide ON*, one-step convergence, and `repairWindow == 7 days`) + `ScreenShareRepairIntegrationTests` (ports + fake Dock + isolated `UserDefaults` suite: record tracks the hide, write-ahead and write-behind ordering incl. both failed-write cases, the kill-then-relaunch repair and the un-poisoned re-hide after it, `.adopt` with `writes == [true]`, settled-preference and already-off discards with **no** Dock write, record preserved when auto-hide is unreadable, manual recovery with no record, manual-failure reporting, round-trip, not-externally-observed, not-registered) + `ScreenShareTerminationTests` (trappable quit) in [ScreenShareTests.swift](../Tests/DockKeeperTests/ScreenShareTests.swift) | Manual §3c, **not yet run — everything on-device is INFERRED**: the real `kill -9` → relaunch cycle (row 3) and the logout path (row 4) are the end-to-end gate on the issue #29 fix; `applicationWillTerminate` firing in the signed bundle (row 2), `SIGTERM` via `TerminationSignals` (row 5), the `.adopt` no-flash property as the far end sees it (row 6), and the menu item for already-poisoned users (row 7). Panic/power-loss durability is UNKNOWN and untestable (row 9); cross-user behavior is argued, not measured (row 10) |
| DK-FR-012 S1–S9 (single-instance guard — ADR-012) | ✅ `InstanceGuard.decide` suite in [InstanceGuardTests.swift](../Tests/DockKeeperTests/InstanceGuardTests.swift): sole instance, self-in-peer-list, newcomer yields, junior ignored, path is never consulted so both duplicate vectors reduce to one decision, pid wrap-around vs start time, dateless newcomer yields, dated beats dateless, **newcomer yields to a settled incumbent in either pid order** (the sequential-incumbency invariant the sweep does not model), `--diagnostics` never pre-empted, exact-match escape hatch, and a 27-case sweep asserting **exactly one survivor** over every start-time shape | Manual matrix §3b, **partially run 2026-08-18** ([#30](https://github.com/blamechris/DockKeeper/issues/30)): rows 6 (both variants), 7, 7b, 7c, 8 and 9 pass. The **peer** start-time substitution is now **CONFIRMED end-to-end by row 7c** — the direct-exec-incumbent direction, which **7b does not cover** (LaunchServices substitutes the incumbent before the guard runs, so nothing is spawned to deflect). Note what the rows can and cannot discriminate, since it is not what it looks like: **row 7 cannot confirm the self substitution at all** — its peer is LS-launched and older, so the newcomer yields whether it ranks `(0, now, pid)` or `(1, .distantPast, pid)`, and a broken `startTime(of: selfPID)` leaves the row green. Row 7c fails only in the *asymmetric* break — peer substitution broken while self substitution works; if **both** break, the two dateless processes tie down to pid order and the incumbent's lower pid happens to save it. The pid-order hazard that exposes is covered in the unit sweep, not here. Item 1 (second account / fast user switching) is **still outstanding but no longer gates the shipped guard** — since the 2026-08-18 ADR-012 amendment the guard filters peers by uid, so per-session correctness is a unit-tested invariant (`InstanceGuard session scoping`, 8 cases, both degradations caught by mutation) rather than an on-device inference. It still gates the `LSMultipleInstancesProhibited` follow-on. The `NSRunningApplication` read and the `exit()` in `SingleInstance` remain un-unit-tested (the last ends the process). **S7 (unbundled builds outside the guard) is now CONFIRMED on-device by row 9** in both its halves — neither blocked nor detected — and the §3b registration note records the single measured cause of both: an unbundled binary checks in with LaunchServices under a **null** bundle identifier, so the guard's early return and its invisibility to `runningApplications(withBundleIdentifier:)` are one fact, not two. **Row 8 CONFIRMS the ADR-012 Consequence that the guard never reaches `dockkeeper-cli`**, against the live Dock and with the app's pid unchanged across the change and its restore |
| DK-NFR-001 (budget) | — | Reliability suite measurements (M6 gate) |
| DK-NFR-002 / DK-PRIV-001 | CONFIRMED by construction (no networking code) | CI symbol check; release network-monitor pass; log-content review |
| Display identity (ADR-004) | ✅ `DisplayIdentityTests` (score table, tie→ambiguous, repair rewrite, migration incl. `cg-` discard, legacy mirror) | Threshold tuning against real hardware (M6) |

## Gaps and sequencing

~~The recovery coordinator and `DockAdapter` seam~~ — landed with tests 2026-07-22 (M3/M4). ~~Fingerprint identity~~ — landed with tests 2026-07-23 (M2). Every P0/P1 decision path now has unit coverage (61 tests); what remains is integration-level polish (CLI argument table, `status` snapshot, poll tick with a real timer) and the manual matrix (M6), the only level gated on hardware.
